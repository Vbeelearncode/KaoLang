// Command.h - Enum for the possible commands in KaoLang
#pragma once

enum class Command {
    SHIFT_LEFT,
    SHIFT_RIGHT,
    NAND,
    LOOP_START,
    LOOP_END,
    WRITE,
    READ,
    MOVE_LEFT,
    MOVE_RIGHT,
    COPY_TO_REGISTER
};